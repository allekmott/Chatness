# This file is generated by the Chatness server
# upon launching ./server --config and providing
# input.

# Database address (can be hostname or IP)
DB_ADDR=127.0.0.1

# Database username (hashed from input)
DB_USER=hashtaghashtag

# Database password (hashed from input)
DB_PASS=hashtaghashtag